export { getSafeEnv } from "@/lib/utils"
export {
  VERSION,
  GIT_SHA,
  BUILD_VERSION,
  NODE_ENV,
  isLocal,
  isDevelopment,
  isTest,
  isStaging,
  isProduction,
  type NodeEnv,
} from "@/lib/constants"
